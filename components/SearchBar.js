view SearchBar {
  <section>
    <label>Search</label>
    <input type='text'/>
  </section>

  $ = {
    backgroundColor: window.color.searchbar,
    height: '75px',
    width: '100%',
    paddingTop: '5px',
  }

  $section = {
    width: '300px',
    paddingLeft: '10px',
  }
}
