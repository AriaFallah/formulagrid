view SearchBar {
  <section>
    <div>Search</div>
    <input type='text'/>
  </section>

  $ = {
    backgroundColor: window.color.searchbar,
    height: '75px',
    width: '100%',
    paddingTop: '5px',
  }

  $section = {
    width: '10%',
    margin: 'auto',
    textAlign: 'center',
  }
}
